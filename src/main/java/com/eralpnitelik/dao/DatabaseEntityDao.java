package com.eralpnitelik.dao;

import com.eralpnitelik.CRUDable;
import com.eralpnitelik.entity.DatabaseEntity;

public class DatabaseEntityDao implements CRUDable<DatabaseEntity>
{
	/*
		No methods required as all methods are default.
	
		Available methods:
	
		create(Entity entity)
		creates new entity on db.
	
		update(long oid, Entity entity)
		updates entity with the given oid with the given entity.
	
		delete(long oid)
		deletes entity with the given oid.
	
		find(long oid)
		returns entity with the given oid.
	
		listAll()
		returns an ArrayList of all entities on db.
	*/	
}
